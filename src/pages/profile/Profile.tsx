import React from 'react';
import Tabs from '../../components/reusableComponents/Tabs';

const Profile = () => {
    return (
        <div>
            <div className="flex">
                <Tabs />
            </div>
        </div>
    );
};

export default Profile;
